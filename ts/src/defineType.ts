import { User } from '../types/index';
import { XXX, YYY } from 'xmodules';
import { CSS } from 'css.module.css';

const hong: User = { id: 1, name: 'Hong' };
const sales: Dept = { id: 100, dname: 'Sales' };

console.log(XXX, YYY, CSS.bold);

let x;
